name: Test Branch
on:
  workflow_dispatch:

env:
  RACK_ENV: stage
  ENV_FILE: .env.stage

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      RACK_ENV: ${{ env.RACK_ENV }}
      ENV_FILE: ${{ env.ENV_FILE }}
      GIT_SHA: ${{ github.sha }}
